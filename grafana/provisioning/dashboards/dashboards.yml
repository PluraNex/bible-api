apiVersion: 1

providers:
  - name: 'Bible API Dashboards'
    orgId: 1
    folder: 'Bible API'
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      recursive: true
      include: ['*.json']
