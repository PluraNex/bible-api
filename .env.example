# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DATABASE_URL=postgresql://bible_user:bible_pass@localhost:5432/bible_api
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bible_api
DB_USER=bible_user
DB_PASSWORD=bible_pass

# Redis Configuration
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# API Configuration
API_VERSION=v1
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# CORS Settings (for development)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Logging
LOG_LEVEL=INFO

# Storage (for future use)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=

# Email (for future use)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Monitoring (for future use)
SENTRY_DSN=

# Audio/TTS (for future use)
AUDIO_ON_DEMAND_ENABLED=True
AUDIO_PROVIDER=polly
AUDIO_DEFAULT_VOICE=Joanna
AUDIO_CDN_BASE_URL=
# OpenAI API (for RAG embeddings and LLM tools)
OPENAI_API_KEY=

# RAG configuration
RAG_ALLOWED_VERSIONS=PT_NAA,PT_ARA,PT_NTLH,EN_KJV
EMBEDDING_BATCH_SIZE=128
